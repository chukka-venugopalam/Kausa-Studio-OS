Using the Kausa episode structure (cold open on the surprising node,
then node-turn-node-turn-payoff, then a one-line season-progress
tie-in, then a single CTA), write a 35-45 second narration script for
this verified chain: [PASTE CHAIN + SOURCES]. Open with the second,
more surprising node -- never the first, obvious one. One idea per
sentence. State nothing not present in the sourced chain.
